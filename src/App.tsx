import React from "react";

import RepeatableFieldForm from './RepeatableFieldForm';

const App: React.FC = () => {
  return (
    <div className="App">
      <RepeatableFieldForm />
    </div>
  );
};

export default App;
